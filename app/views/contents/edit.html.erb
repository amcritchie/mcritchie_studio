<section>
  <div class="mb-6">
    <%= link_to "&larr; Back".html_safe, content_path(@content.slug), class: "text-secondary hover:text-heading text-sm transition" %>
  </div>

  <div class="max-w-2xl">
    <h2 class="text-2xl font-bold text-heading mb-6">Edit Content</h2>
    <%= render "form", content: @content %>
  </div>
</section>
