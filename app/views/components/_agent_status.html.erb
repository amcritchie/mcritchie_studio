<%# locals: (agent:) %>
<span class="inline-block w-2 h-2 rounded-full <%= agent.status == 'active' ? 'bg-mint' : 'bg-surface-alt' %>"></span>
<span class="text-xs text-secondary"><%= agent.status.capitalize %></span>
